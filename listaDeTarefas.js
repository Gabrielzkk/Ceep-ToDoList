const addNovaTarefa = document.querySelector("[data-form-button]");

addNovaTarefa.addEventListener("click", (event) => {

    event.preventDefault();
    console.log("teste de clique");
});
console.log(addNovaTarefa)